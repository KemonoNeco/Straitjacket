# The POSIX launcher must keep LF endings — a CRLF shebang breaks `/bin/sh` on
# Unix ("bad interpreter"). The prebuilt binaries are binary blobs.
bin/straitjacket        text eol=lf
bin/straitjacket-*      binary
*.sh                      text eol=lf
